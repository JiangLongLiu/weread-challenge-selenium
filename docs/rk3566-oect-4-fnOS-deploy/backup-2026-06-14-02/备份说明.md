# 远程主机配置备份（改造后版本）

> 主机：192.168.1.54（RK3566-OECT1-4-fnOS）
> 备份时间：2026-06-14（crontab 改造后）
> 内核：Linux 6.18.18-trim (aarch64)

## 备份文件清单

| 文件名 | 来源路径 | 说明 |
|---|---|---|
| crontab.bak | `crontab -l` | **改造后** crontab（2 用户错峰 + @reboot 恢复 + 02:25~05:35 窗口） |
| weread-selenium.service | `/etc/systemd/system/weread-selenium.service` | 开机自启 Selenium 的 systemd 服务（**已禁用**） |
| docker.service | `/etc/systemd/system/docker.service` | fnOS 定制的 Docker daemon 服务 |
| dockermgr.service | `/etc/systemd/system/dockermgr.service` | fnOS Docker 管理服务 |
| docker-compose.yml | `/vol1/docker/mycontainers/weread-challenge-selenium-muti-user/docker-compose.yml` | 微信读书多用户容器编排文件（未改动，仍含 4 用户） |

## 与 backup-2026-06-14 的差异

| 项目 | backup-2026-06-14（改造前） | backup-2026-06-14-02（改造后） |
|---|---|---|
| crontab | 4 用户 × 4 窗口，sleep 4200 | 2 用户错峰，app-1 sleep 5400 / app-2 sleep 5280 + @reboot |
| weread-selenium.service | enabled, active | **disabled, inactive** |
| docker-compose.yml | 不变 | 不变 |
| docker.service | 不变 | 不变 |
| dockermgr.service | 不变 | 不变 |

## 远程主机镜像清单（备份时）

| 镜像 | 标签 | 大小 |
|---|---|---|
| selenium/standalone-chromium | 147.0 | 2.17 GB |
| jqknono/weread-challenge | v0.15.0 | 192 MB |
| selenium/standalone-chromium | 144.0（旧版） | 2.16 GB |

## 远程主机 weread 相关容器状态（备份时）

| 容器名 | 状态 |
|---|---|
| weread-challenge-selenium-muti-user | **已停止**（非运行窗口 02:25~05:35） |
| app-1 ~ app-4 | **已停止** |

## systemd 服务状态（备份时）

- `weread-selenium.service` — **disabled**, **inactive**（已禁用开机自启）
- `docker.service` — active running
- `dockermgr.service` — active running

## 回滚方法

如需回滚到改造前配置：

```bash
# 1. 恢复旧 crontab
crontab /path/to/backup-2026-06-14/crontab.bak

# 2. 重新启用 systemd 开机自启
systemctl enable weread-selenium.service
systemctl start weread-selenium.service
```
