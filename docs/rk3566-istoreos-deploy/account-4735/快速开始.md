# 快速开始指南

本指南帮助你在 5 分钟内完成 RK3566 iStoreOS 上的微信读书助手部署。

## 环境要求

| 项目 | 要求 |
|------|------|
| 硬件 | RK3566 开发板 (如 R66S/R68S) |
| 系统 | iStoreOS |
| 网络 | 可访问互联网 |

## 部署步骤

### 第 1 步：准备 password.csv

在 `docs/rk3566-istoreos-deploy/account-4735/password.csv` 中填写 SSH 密码：

| 列名 | 示例值 |
|------|--------|
| IP地址 | 192.168.123.51 |
| 用户名 | root |
| 密码 | 1234567890 |
| SSH端口 | 22 |
| 备注 | RK3566主机1 |

### 第 2 步：安装依赖

```bash
pip install pandas paramiko pyyaml
```

### 第 3 步：执行部署

```bash
python docs/rk3566-istoreos-deploy/account-4735/scripts/ssh_scp_util.py
```

脚本会自动：
1. 连接 SSH
2. 创建部署目录
3. 清理旧定时任务
4. 上传 docker-compose.yml 和 setup-cron.sh
5. 配置定时任务（每 6 小时自动阅读）
6. 启动容器

### 第 4 步：扫码登录

1. 打开 VNC 界面：http://192.168.123.51:7900
2. 输入 VNC 密码：**111111**
3. 在浏览器中打开微信读书，扫描 data/login.png 二维码登录

## 验证部署

```bash
# 查看容器状态
ssh root@192.168.123.51 "docker ps | grep weread"
```

正常输出：
```
weread-challenge-selenium-app-1      Up
weread-challenge-selenium-selenium-1   Up (healthy)
```

## 常见问题

### Q: VNC 密码不对？
A: 修改 docker-compose.yml 中的 `SE_VNC_PASSWORD` 后重新部署

### Q: 容器启动失败？
A: 检查 Selenium 是否健康：`docker ps | grep selenium`

### Q: 如何修改阅读时长？
A: 编辑 docker-compose.yml 中的 `WEREAD_DURATION=68`（分钟）

## 每日运行

部署完成后，定时任务会自动每 6 小时启动阅读。

### 状态检查
```bash
python docs/rk3566-istoreos-deploy/account-4735/scripts/check-status.py
```

### 查看日志
ssh root@192.168.123.51 "docker logs weread-challenge-selenium-app-1 --tail 20"
```
